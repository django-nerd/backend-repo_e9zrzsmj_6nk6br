# Schemas are not needed for the current app because the contact form uses mailto: and does not hit the backend.
# Keeping this file minimal to avoid confusion while leaving space for future extensions.
from pydantic import BaseModel  # noqa: F401
